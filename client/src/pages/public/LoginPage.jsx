export default function LoginPage() {
  return (
    <section className="page-stack">
      <h2>Login</h2>
      <p>Connect this page to the `/api/auth/login` endpoint next.</p>
    </section>
  );
}
